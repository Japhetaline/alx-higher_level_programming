#!/usr/bin/python3

def divisible_by_2(my_list=[]):
    """Find all multiples of 2 in a list"""
    japh_list = []
    for k in range(len(my_list)):
        if my_list[k] % 2 == 0:
            japh_list.append(True)
        else:
            japh_list.append(False)

            return (japh_list)
